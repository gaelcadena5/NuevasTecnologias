// Configuración global del Frontend
const CONFIG = {
  // URL base de la API del Backend.
  // En desarrollo local apunta al puerto predeterminado de Express (5000).
  // En producción, se puede cambiar por la URL del balanceador de carga o gateway (ej: https://api.mi-dominio.com/api)
  API_BASE_URL: 'https://ytznpxnsqq.us-east-1.awsapprunner.com/api'
};

export default CONFIG;
